TITLE
    Creating a User (Invalid Username, length)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the create 
    transaction. Enter an invalid username (which is too long, >15 characters) 
    and verify that the username is rejected and that an error occurs.
DEPENDENCIES
    create_1
