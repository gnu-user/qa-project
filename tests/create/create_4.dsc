TITLE
    Creating a User (Invalid Username, invalid name)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the create 
    transaction. Enter an invalid username (which contains “END “) and verify 
    that the username is rejected and that an error occurs.
DEPENDENCIES
    login Test Suite
    create_1
