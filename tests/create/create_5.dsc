TITLE
    Creating a User (Invalid Username, uniqueness)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the create 
    transaction. Enter a valid username (which already exists) and verify that 
    the username is rejected and that an error occurs.
DEPENDENCIES
    login Test Suite
    create_1
