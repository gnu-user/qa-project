TITLE
    Creating a User (User Type: buy-standard)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the create 
    transaction. Enter a valid username when prompted. Enter the buy-standard 
    user type. Verify that the user type is accepted and the user has been 
    created in the daily transaction file.
DEPENDENCIES
    create_6
