TITLE
    Delete User Input (Current User)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the delete 
    transaction, and enter the current active username. Verify that the 
    username entered is rejected, and an error occurs.
DEPENDENCIES
    delete_1
