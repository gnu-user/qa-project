TITLE
    Login Sequence of Events
DESCRIPTION
    Verify that no transactions are accepted before login by testing each of 
    the specified transactions (except for login) prior to running the login 
    transaction. Each transaction attempted should produce an error.
DEPENDENCIES
