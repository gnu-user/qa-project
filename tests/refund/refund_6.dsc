TITLE
    Refund Seller Input (Valid Input, seller exists)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the refund 
    transaction. When prompted, enter a buyer which exists. When prompted, 
    enter a seller which exists. Verify that the seller is accepted without 
    error.
DEPENDENCIES
    refund_4
