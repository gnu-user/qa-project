TITLE
    Refund Amount Prompt
DESCRIPTION
    Run the login transaction using an admin account. Initiate the refund 
    transaction. When prompted, enter a buyer which exists. When prompted, 
    enter a seller which exists. Verify that the user is prompted to enter a 
    refund amount.
DEPENDENCIES
    refund_6
