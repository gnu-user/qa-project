TITLE
    Refund Amount Input (Invalid input, numerical)
DESCRIPTION
    Run the login transaction using an admin account. Initiate the refund 
    transaction. When prompted, enter a buyer which exists. When prompted, 
    enter a seller which exists. When prompted, enter an integer value (without 
    a decimal) for the refund amount. Verify that the value is rejected and an 
    error occurs.
DEPENDENCIES
    refund_7
